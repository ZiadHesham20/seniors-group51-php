<?php
//1)$sql = "SELECT department_id , AVG(salary) FROM employees GROUP BY department_id;";
//2)$sql = "SELECT job_id, COUNT(*) FROM employees GROUP BY job_id;";
//3)$sql = "SELECT YEAR(NOW()) - YEAR(`hire_date`) FROM employees;";
//4)$sql = "SELECT `job_id`,`department_id`,`commission_pct` FROM employees WHERE commission_pct IS NOT NULL GROUP BY `department_id`;";
//5)SELECT first_name,manager_id as workUnderSteven,employee_id FROM `employees` WHERE `manager_id` is NOT NUll;
